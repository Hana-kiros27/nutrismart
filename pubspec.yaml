name: nutrismart
description: Smart Nutrition Planner
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  hive:
  hive_flutter:

  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  intl: ^0.19.0
  uuid: ^4.5.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.5.1
  cloud_firestore: ^6.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator:
  build_runner:
  
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/